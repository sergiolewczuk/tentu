security:
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        in_memory: { memory: ~ }
        our_db_provider:
            entity:
                class: App\Entity\Cliente
                property: correo
    encoders:
        App\Entity\Cliente:
            algorithm: bcrypt
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            pattern:    ^/
            form_login:
                login_path: login
                check_path: login
                always_use_default_target_path: true
            provider: our_db_provider
            logout:
                path:   logout
                target: login
    role_hierarchy:
        ROLE_ADMIN: [ROLE_MODERADOR, ROLE_ESPECTADOR, ROLE_CLIENTE]
        ROLE_MODERADOR: [ROLE_ESPECTADOR, ROLE_CLIENTE]
        ROLE_CLIENTE: [ROLE_ESPECTADOR]
                
            # activate different ways to authenticate

            # http_basic: true
            # https://symfony.com/doc/current/security.html#a-configuring-how-your-users-will-authenticate

            # form_login: true
            # https://symfony.com/doc/current/security/form_login_setup.html

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # - { path: ^/admin, roles: IS_AUTHENTICATED_ANONYMOUSLY, requires_channel: https }
        # - { path: ^/, roles: IS_AUTHENTICATED_ANONYMOUSLY, requires_channel: https }
        # - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
